machine:
  node:
    version: 5.4.0
dependencies:
  pre:
    - npm install -g bower
  override:
    - npm i
    - bower i
deployment:
  production:
    branch: master
    heroku:
      appname: barberscore-web
