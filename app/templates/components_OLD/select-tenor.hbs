{{#power-select
      options=filteredRoles
      selected=role
      placeholder="Tenor"
      onchange=(action (mut role))
      allowClear=true
      as |role|
}}
  {{role.person.name}}
{{/power-select}}
