{{#power-select
  options=organizationChoices
  selected=organization
  allowClear=true
  placeholder='Select Organization'
  searchField='name'
  onchange=(action (mut organization))
  as |organization|
}}
  {{organization.name}}
{{/power-select}}
