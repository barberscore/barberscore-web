{{#power-select
      options=rounds
      selected=current
      placeholder="Select Current Round"
      onchange=(action "saveCurrent")
      allowClear=true
      as |current|
}}
  {{current.name}}
{{/power-select}}
