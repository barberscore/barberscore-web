{{#power-select
      options=sortedContestants
      selected=champion
      allowClear=true
      placeholder='Champion'
      onchange=(action "saveRecord")
      as |champion|
}}
  {{champion}}
{{/power-select}}
