{{#power-select
  options=organizationChoices
  selected=representing
  placeholder='Select Organization'
  searchField='name'
  onchange=(action (mut representing))
  as |representing|
}}
  {{representing.name}}
{{/power-select}}
