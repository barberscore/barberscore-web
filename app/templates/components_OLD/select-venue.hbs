  {{#power-select-with-create
      search=(action "searchVenue")
      selected=venue
      placeholder='Venue'
      allowClear=true
      onchange=(action (mut venue))
      oncreate=(action "createVenue")
      as |venue term|
}}
  {{venue.name}}
{{/power-select-with-create}}
