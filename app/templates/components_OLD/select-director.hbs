{{#power-select
      options=filteredRoles
      selected=role
      placeholder="Director"
      onchange=(action (mut role))
      allowClear=true
      as |role|
}}
  {{role.person.name}}: {{role.start-date}}
{{/power-select}}
