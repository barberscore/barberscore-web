<div>
  <table class='table'>
    <thead>
      <tr>
        <th>Name</th>
      </tr>
    </thead>
    <tbody>
      {{#each model as |person|}}
        <tr>
          <td>
            {{#link-to 'persons.person' person}}{{person.name}}{{/link-to}}
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
</div>
