{{#bs-button
  type='primary'
  disabled=model.isDisabled
  onClick=(action (mut publishRoundModal) true)
}}
  Publish Round
{{/bs-button}}
{{#bs-modal
  open=publishRoundModal
  onHidden=(pipe-action (action (mut publishRoundModal) false) (action (mut publishRoundModalError) false))
  as |modal|
}}
  {{#modal.header}}
    <h4>Publish Round</h4>
  {{/modal.header}}
  {{#modal.body}}
    <p>
        This ends the round and makes the results public.  It generates and emails the final versions of the OSS, CSA, and SA reports for this round (where applicable.)
    </p>
    <h4>
      The Round should not be "Published" until the announcements are over!
    </h4>
    {{#if publishRoundModalError}}
    <p class='text-danger'>
      Could not Publish Round.  Please ensure all Manual Award Winners have been selected.  Otherwise, contact support.
    </p>
    {{/if}}
  {{/modal.body}}
  {{#modal.footer}}
    {{#bs-button onClick=(action modal.close) type="default"}}Cancel{{/bs-button}}
    {{#bs-button type='primary' disabled=model.isError onClick=(perform publishRound)}}
      {{#if publishRound.isRunning}}
        Publishing {{fa-icon "spinner" spin=true}}
      {{else}}
        Publish Round
      {{/if}}
    {{/bs-button}}
  {{/modal.footer}}
{{/bs-modal}}