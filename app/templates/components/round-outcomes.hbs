{{#unless (eq model.status 'New')}}
  <div class='table-responsive'>
    <table class='table'>
        <caption>
          Outcomes refers to the awards that will be announced out at the conclusion of this particular round.  Winners are determined at Round Verification and can be overriden by the CA if necessary.  <strong>NOTE:</strong> Awards that have a checkmark under the "Manual" column must be manually entered by the CA.
        </caption>
      <thead>
        <tr>
          <th class='col-md-1'>Num</th>
          <th class='col-md-5'>Contest</th>
          <th class='col-md-1'>Manual</th>
          <th class='col-md-4'>Result</th>
        </tr>
      </thead>
      <tbody>
        {{#each sortedOutcomes as |outcome|}}
            <tr>
              <td>
                {{outcome.num}}
              </td>
              <td>
                {{#link-to 'dashboard.conventions.convention.sessions.session.rounds.round.outcomes.outcome' outcome}}{{outcome.award.name}}{{/link-to}}
              </td>
              <td>
                {{#if (eq outcome.award.level 'Manual')}}
                  {{fa-icon 'check-circle' class='text-success'}}
                {{/if}}
              </td>
              <td>
                {{outcome.name}}
              </td>
            </tr>
        {{else}}
          <tr>
            <td colspan=3>No award outcomes this round.</td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
{{else}}
  <h4>
    Outcomes are available after the Round is Verified.
  </h4>
{{/unless}}