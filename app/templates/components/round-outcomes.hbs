{{#unless (eq model.status 'New')}}
  <div class='table-responsive'>
    <table class='table'>
        <caption>
          Outcomes are determined at Round Verification as appropriate (for the round, multi-round contestants, etc.)  The CA can override all determinations if necessary.  Only those contests with winners selected will appear on the announcements and OSS.
        </caption>
      <thead>
        <tr>
          <th class='col-md-1'>Num</th>
          <th class='col-md-5'>Contest</th>
          <th class='col-md-1'>Manual</th>
          <th class='col-md-4'>Result</th>
        </tr>
      </thead>
      <tbody>
        {{#each sortedOutcomes as |outcome|}}
            <tr>
              <td>
                {{outcome.num}}
              </td>
              <td>
                {{#link-to 'dashboard.conventions.convention.sessions.session.rounds.round.outcomes.outcome' outcome}}{{outcome.contest.award.name}}{{/link-to}}
              </td>
              <td>
                {{#if (eq outcome.contest.award.level 'Manual')}}
                  {{fa-icon 'check-circle' class='text-success'}}
                {{/if}}
              </td>
              <td>
                {{outcome.name}}
              </td>
            </tr>
        {{else}}
          <tr>
            <td colspan=3>No contests.</td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
{{else}}
  <h4>
    Contests are available after the Round is Verified.
  </h4>
{{/unless}}
