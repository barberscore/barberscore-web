<section>
  <div class='card'>
    <div class='card-header'>
      {{prev-next model=model sortedArray=sortedContests}}

      <h4>
        {{model.award.name}}
      </h4>
    </div>
    <div class='card-body'>
      <div class='table-responsive'>
        <table class='table'>
          <h6 class='card-title'>Contestants: {{model.includedContestantsCount}}</h6>
          <thead>
            <tr>
              <th>Name</th>
            </tr>
          </thead>
          <tbody>
            {{#each sortedContestants as |contestant|}}
              <tr>
                <td>
                  {{contestant.entry.group.name}}
                </td>
              </tr>
            {{else}}
              <tr>
                <td>(No contestants entered for this contest.)</td>
              </tr>
            {{/each}}
          </tbody>
        </table>
      </div>
    </div>
  </div>
</section>