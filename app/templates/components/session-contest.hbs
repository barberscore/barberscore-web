<section>
  <div class='card'>
    <div class='card-header'>
      {{prev-next model=model sortedArray=sortedContests}}

      <h4>
        {{html-safe model.name}}
      </h4>
    </div>
    <div class='card-body'>
      <div class='table-responsive'>
        <table class='table table-hover table-sm'>
          <caption>Groups: {{model.entriesCount}}</caption>
          <thead class='thead-light'>
            <tr>
              <th>Name</th>
            </tr>
          </thead>
          <tbody>
            {{#each sortedEntries as |entry|}}
              <tr>
                <td>
                  {{html-safe entry.name}}
                </td>
              </tr>
            {{else}}
              <tr>
                <td>(No groups entered for this contest.)</td>
              </tr>
            {{/each}}
          </tbody>
        </table>
      </div>
    </div>
  </div>
</section>