{{#power-select
      options=filteredRoles
      selected=role
      placeholder="Director"
      onchange=(action (mut role))
      allowClear=true
      as |role|
}}
  {{role.person.name}}: {{date-range role.date}}
{{/power-select}}
