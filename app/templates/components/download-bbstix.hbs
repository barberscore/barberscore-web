<BsButton
  @type='success'
  @onClick={{action 'downloadBbstix' this.bbstix}}
>
  {{#if this.isLoading}}
    Downloading <FaIcon @icon="spinner" @spin={{true}} />
  {{else}}
    Download BBSTIX&nbsp;&nbsp;<FaIcon @icon='file-text' />
  {{/if}}
</BsButton>