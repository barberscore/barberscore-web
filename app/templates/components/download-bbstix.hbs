<BsButton
  @type='success'
  @onClick=(perform bbstix)
>
  {{#if bbstix.isRunning}}
    Downloading <FaIcon @icon="spinner" />
  {{else if bbstix.last.isError}}
    Error Downloading
  {{else}}
    Download BBSTIX&nbsp;&nbsp;<FaIcon @icon='file-text-o' />
  {{/if}}
</BsButton>