{{#power-select
      options=riserChoices
      selected=risers
      allowClear=true
      placeholder='Number of Risers'
      onchange=(action (mut risers))
      as |risers|
}}
  {{risers}}
{{/power-select}}
