{{#power-select
      options=filteredRoles
      selected=role
      placeholder="Baritone"
      onchange=(action (mut role))
      allowClear=true
      as |role|
}}
  {{role.person.name}}
{{/power-select}}
