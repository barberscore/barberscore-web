<section>
  Round Date: {{!-- {{#if model.date}}{{moment-format model.date 'LL'}}{{else}}(Unknown){{/if}} --}}
    {{bs-datetimepicker
      date=model.date
      format="LL"
      showIcon=true
      isTime=false
      disabled=isDisabled
      allowInputToggle=true
      change=(perform autosave)
    }}
  <small><em>If the round takes place across multiple days, set the round date to the last day of the round.</em></small>
</section>
<section>
  {{round-footnotes model=model}}
</section>
{{#unless (eq model.kind 'Finals')}}
<section>
  {{round-spots model=model}}
</section>
{{/unless}}