<BsButton
  @type='success'
  @onClick={{perform this.csa}}
>
  {{#if this.csa.isRunning}}
    Downloading <FaIcon @icon="spinner" @spin={{true}} />
  {{else if this.csa.last.isError}}
    Error Downloading
  {{else}}
    Download CSA&nbsp;&nbsp;<FaIcon @icon='file-pdf' />
  {{/if}}
</BsButton>