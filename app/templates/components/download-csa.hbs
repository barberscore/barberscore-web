{{#bs-button
  type='success'
  onClick=(perform this.csa)
}}
  {{#if this.csa.isRunning}}
    Downloading {{fa-icon "spinner" spin=true}}
  {{else if this.csa.last.isError}}
    Error Downloading
  {{else}}
    Download CSA&nbsp;&nbsp;{{fa-icon 'file-pdf-o'}}
  {{/if}}
{{/bs-button}}