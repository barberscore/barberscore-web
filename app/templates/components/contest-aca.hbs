{{#power-select
      search=(action "searchPerson")
      selected=person
      placeholder='Assistant Contest Administrator'
      onchange=(action "saveRecord")
      as |person|
}}
  {{person.name}}
{{/power-select}}
