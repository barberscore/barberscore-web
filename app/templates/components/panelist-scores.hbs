{{#each sortedScores as |score|}}
  {{input
    class='form-control text-right big-num'
    type='tel'
    value=score.points
    min=0
    max=100
    disabled=isDisabled
    change=(perform autosave score element='target.id')
  }}
{{/each}}