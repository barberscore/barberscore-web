{{#power-select
      options=filteredRoles
      selected=role
      placeholder="Bass"
      onchange=(action (mut role))
      allowClear=true
      as |role|
}}
  {{role.person.name}}
{{/power-select}}
