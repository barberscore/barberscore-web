{{#power-select
      options=sortedRoles
      selected=role
      placeholder="Bass"
      onchange=(action "saveRecord")
      allowClear=true
      as |role|
}}
  {{role.person.name}}
{{/power-select}}
