{{!-- {{#bs-button
  type='success'
  onClick=(perform psa)
}}
  {{#if psa.isRunning}}
    Downloading <FaIcon @icon="spinner" @spin={{true}} />
  {{else if psa.last.isError}}
    Error Downloading
  {{else}}
    Download PSA&nbsp;&nbsp;<FaIcon @icon='file-text' />
  {{/if}}
{{/bs-button}} --}}