{{#unless isProduction}}
  {{#bs-button
    type='outline-dark'
    disabled=model.isDisabled
    onClick=(perform mockRound)
  }}
    {{#if mockRound.isRunning}}
      Mocking... {{fa-icon "spinner" spin=true}}
    {{else}}
      Mock Round
    {{/if}}
  {{/bs-button}}
{{/unless}}