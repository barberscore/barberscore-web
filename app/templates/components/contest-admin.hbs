{{#power-select
      search=(action "searchPerson")
      selected=person
      placeholder='Contest Administrator'
      onchange=(action "saveRecord")
      as |person|
}}
  {{person.name}}
{{/power-select}}
