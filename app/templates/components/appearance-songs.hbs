<h6>Songs</h6>
<div class='table-responsive'>
  <table class='table table-hover table-sm'>
    <thead class='thead-light'>
      <tr>
        <th>Name</th>
      </tr>
    </thead>
    <tbody>
      {{#each sortedSongs as |song|}}
        <tr>
          <td>
            {{#power-select
              options=model.charts
              selected=song.title
              onchange=(perform autosave song)
              placeholder="Choose Chart Title"
              searchField='title'
              allowClear=false
              disabled=false
              as |chart|
            }}
              Song {{song.num}}: {{chart}}
            {{/power-select}}
          </td>
        </tr>
      {{else}}
        <tr>
          <td>(No songs.)</td>
        </tr>
      {{/each}}
    </tbody>
  </table>
</div>