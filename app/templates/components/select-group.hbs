{{#power-select-with-create
  search=(action "searchGroup")
  selected=group
  placeholder='Select Group'
  allowClear=true
  onchange=(action (mut group))
  oncreate=(action "createGroup")
  as |group term|
}}
  {{group.name}}
{{/power-select-with-create}}
