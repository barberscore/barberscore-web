{{#bs-button
  type='success'
  onClick=(perform oss)
}}
  {{#if oss.isRunning}}
    Downloading {{fa-icon "spinner" spin=true}}
  {{else if oss.last.isError}}
    Error Downloading
  {{else}}
    Download OSS&nbsp;&nbsp;{{fa-icon 'file-pdf-o'}}
  {{/if}}
{{/bs-button}}