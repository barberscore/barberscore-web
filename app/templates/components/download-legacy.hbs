<BsButton
  @type='success'
  @onClick=(perform legacy)
>
  {{#if this.legacy.isRunning}}
    Downloading <FaIcon @icon="spinner" />
  {{else if this.legacy.last.isError}}
    Error Downloading
  {{else}}
    Download Legacy Report&nbsp;&nbsp;<FaIcon @icon='file-excel-o' />
  {{/if}}
</BsButton>