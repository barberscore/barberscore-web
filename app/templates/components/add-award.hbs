{{#bs-form formLayout="inline" action="saveAward" class='form-block'}}
  {{#bs-form-group}}
    {{#power-select
      options=availableAwards
      placeholder='Search by Award Name (if qualifier, search for parent award)'
      searchField='nomen'
      allowClear=true
      selected=award
      onchange=(action (mut award))
      as |award|
    }}
      {{award.nomen}}
    {{/power-select}}
  {{/bs-form-group}}
  {{bs-button defaultText="Add" type="primary" buttonType="submit"}}
{{/bs-form}}
{{hosts}}