{{#power-select
      search=(perform searchTask)
      selected=award
      onchange=(action (mut award))
      placeholder="Search by Award Name"
      allowClear=true
      as |award|
}}
  {{award.nomen}}
{{/power-select}}
{{#bs-button action=(action 'saveAward') class='btn btn-primary'}}Add{{/bs-button}}
