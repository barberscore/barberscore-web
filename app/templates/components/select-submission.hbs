{{#power-select
      options=submissionChoices
      selected=submission
      placeholder="Select Song"
      onchange=(action "saveSubmission")
      allowClear=true
      as |submission|
}}
  {{submission.title}}
{{/power-select}}
