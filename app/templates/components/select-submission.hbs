{{#power-select-with-create
      options=submissionChoices
      selected=submission
      placeholder="Select Song"
      onchange=(action "saveSubmission")
      oncreate=(action "createSubmission" term)
      allowClear=true
      as |submission term|
}}
  {{submission.chart.name}}
{{/power-select-with-create}}
