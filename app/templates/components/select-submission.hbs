{{#power-select
  options=options
  selected=selected
  onchange=(action "saveRecord")
  as |submission|
}}
  {{submission.chart.name}}
{{/power-select}}
  {{!-- search=(action "searchSubmission") --}}
