<BsButton
  @disabled={{this.model.isDisabled}}
  @type='primary'
  @onClick={{perform this.startAppearance}}
>
  {{#if this.startAppearance.isRunning}}
    Starting <FaIcon @icon="spinner" @spin={{true}} />
  {{else}}
    Start Performance
  {{/if}}
</BsButton>