{{#bs-button
  disabled=model.isDisabled
  type='primary'
  onClick=(perform startAppearance)
}}
  {{#if startAppearance.isRunning}}
    Starting {{fa-icon "spinner" spin=true}}
  {{else}}
    Start Performance
  {{/if}}
{{/bs-button}}
