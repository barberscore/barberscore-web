{{#power-select
      options=seasonChoices
      disabled=disabled
      selected=season
      placeholder="Select Season"
      onchange=(action (mut season))
      allowClear=true
      as |season|
}}
  {{season}}
{{/power-select}}
