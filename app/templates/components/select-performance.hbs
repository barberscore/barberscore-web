{{#power-select
      options=performances
      selected=cursor
      placeholder="Select Cursor Performance"
      onchange=(action "saveCursor")
      allowClear=true
      as |cursor|
}}
  {{cursor.num}}: {{cursor.performer.group.name}}
{{/power-select}}
