{{#power-select
      search=(action "searchPerson")
      selected=person
      placeholder='Contest Administrator'
      onchange=(action (mut person))
      as |person|
}}
  {{person.name}}
{{/power-select}}
