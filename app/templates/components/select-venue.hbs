  {{#power-select
      search=(action "searchVenue")
      selected=venue
      placeholder='Venue'
      allowClear=true
      onchange=(action (mut venue))
      as |venue|
}}
  {{venue.name}}
{{/power-select}}
