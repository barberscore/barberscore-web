  {{#power-select
      search=(action "searchVenue")
      disabled=disabled
      selected=venue
      placeholder='Venue'
      onchange=(action (mut venue))
      as |venue|
}}
  {{venue.name}}
{{/power-select}}
