{{#power-select
      search=(action "searchJudge")
      placeholder="Enter Judge Name"
      selected=person
      onchange=(action "saveRecord")
      as |person|
}}
  {{person.name}}
{{/power-select}}
