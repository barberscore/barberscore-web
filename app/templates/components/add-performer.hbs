{{#power-select
  search=(action "searchGroup")
  placeholder='Create Manual Entry.  (Add Group to Session.)'
  selected=group
  onchange=(action "saveRecord")
  as |group|
}}
  {{group.name}}
{{/power-select}}
