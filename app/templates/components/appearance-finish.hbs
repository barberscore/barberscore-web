<BsButton @disabled={{this.model.isDisabled}} @type='primary' @onClick={{perform this.finishAppearance}}>
  {{#if this.finishAppearance.isRunning}}
    Concluding <FaIcon @icon="spinner" />
  {{else}}
    {{#if (eq this.model.status 'Started')}}
      Conclude Performance
    {{/if}}
  {{/if}}
</BsButton>