{{#if (eq model.status 'New')}}
  <div class='table-responsive'>
    <table class='table'>
      <caption>
        The Panelists (the actual seated judges) are created from the Assignments (who is scheduled).  You can not create the Panelists themselves until the Session has started.  Until then, please contact David Mills or Chris Buechler to change the Assignments. Once the DRCJ starts the Session, you'll be able to add/edit/remove panelists as needed.  Here are the currently confirmed assignments:
      </caption>
      <thead>
        <tr>
          <th>Person</th>
          <th>Category</th>
          <th>Kind</th>
          <th>Status</th>
        </tr>
      </thead>
      <tbody>
        {{#each sortedAssignments as |assignment|}}
          <tr>
            <td>
              {{assignment.person.commonName}}
            </td>
            <td>
              {{assignment.category}}
            </td>
            <td>
              {{assignment.kind}}
            </td>
            <td>
              {{assignment.status}}
            </td>
          </tr>
        {{else}}
          <tr>
            <td>(No assignments.)</td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
{{else}}
  <div class='table-responsive'>
    <table class='table'>
      <caption>
        If you need to change any panelists please do so before you start the Round.  Once the Round is started, you will not be able to change the Panelists.
      </caption>
      <thead>
        <tr>
          <th>Person</th>
          <th>Category</th>
          <th>Kind</th>
        </tr>
      </thead>
      <tbody>
        {{#each sortedPanelists as |panelist|}}
          <tr>
            <td>
              {{#if (eq model.status 'New')}}
                {{#link-to 'dashboard.rounds.round.panelists.panelist' panelist}}{{panelist.person.fullName}}{{/link-to}}
              {{else}}
                {{panelist.person.fullName}}
              {{/if}}
            </td>
            <td>
              {{panelist.category}}
            </td>
            <td>
              {{panelist.kind}}
            </td>
          </tr>
        {{else}}
          <tr>
            <td>(No panelists created.)</td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
  <div>
    <p>
    {{#bs-button type='success' disabled=model.isError onClick=(action (mut createPanelistModal) true)}}Create New Panelist{{/bs-button}}
    </p>
  </div>
  <div>
    {{#bs-modal
      open=createPanelistModal
      onHidden=(pipe-action (action (mut createPanelistModal) false) (action (mut createPanelistModalError) false))
      as |modal|
    }}
      {{#modal.header}}
        <h4>New Panelist</h4>
      {{/modal.header}}
      {{#modal.body}}
        <p>
          Select the person you wish to enter into the panel.
        </p>
        {{#power-select
          search=(perform searchPerson)
          selected=person
          onchange=(action (mut person))
          placeholder="Enter Person Name"
          allowClear=true
          renderInPlace=true
          noMatchesMessage="We Could Not Find a person with that name"
          as |person|
        }}
          Person: {{person.full_name}}
        {{/power-select}}
        {{#if createPanelistModalError}}
        <p class='text-danger'>
          Could not create panelist.  Please contact support.
        </p>
        {{/if}}
      {{/modal.body}}
      {{#modal.footer}}
        {{#bs-button onClick=(action modal.close) type="default"}}Cancel{{/bs-button}}
        {{#bs-button type='success' onClick=(perform savePanelist person) disabled=isDisabled}}
          {{#if savePanelist.isRunning}}
            Creating {{fa-icon "spinner" spin=true}}
          {{else}}
            Create Panelist
          {{/if}}
        {{/bs-button}}
      {{/modal.footer}}
    {{/bs-modal}}
  </div>
{{/if}}
