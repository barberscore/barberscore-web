{{#unless (eq model.status 'New')}}
  <div class='table-responsive'>
    <table class='table'>
        <h6 class='card-title'>
          Contest winners are determined at Round Verification as appropriate (for the round, multi-round contestants, etc.)  The CA can override all determinations if necessary.  Only those contests with winners selected will appear on the announcements and OSS.
        </h6>
      <thead>
        <tr>
          <th class='col-md-1'>Num</th>
          <th class='col-md-5'>Name</th>
          <th class='col-md-1'>Manual</th>
          <th class='col-md-4'>Winner</th>
        </tr>
      </thead>
      <tbody>
        {{#each sortedContests as |contest|}}
            <tr>
              <td>
                {{contest.num}}
              </td>
              <td>
                {{#link-to 'dashboard.conventions.convention.sessions.session.rounds.round.contests.contest' contest}}{{contest.award.name}}{{/link-to}}
              </td>
              <td>
                {{#if (eq contest.award.level 'Manual')}}
                  {{fa-icon 'check-circle' class='text-success'}}
                {{/if}}
              </td>
              <td>
                {{#if contest.group}}
                  {{contest.group.name}}
                {{else}}
                  {{#if (eq contest.award.level 'Manual')}}
                    (Select Manually)
                  {{else if (eq contest.award.level 'Qualfier')}}
                    (Qualifiers Appear on OSS)
                  {{else}}
                    (Determined Automatically)
                  {{/if}}
                {{/if}}
              </td>
            </tr>
        {{else}}
          <tr>
            <td colspan=3>No contests.</td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
{{else}}
  <h4>
    Contests are available after the Round is Verified.
  </h4>
{{/unless}}
