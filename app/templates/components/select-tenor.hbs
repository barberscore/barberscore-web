{{#power-select
      options=sortedRoles
      selected=role
      placeholder="Tenor"
      onchange=(action "saveRecord")
      allowClear=true
      as |role|
}}
  {{role.person.name}}
{{/power-select}}
