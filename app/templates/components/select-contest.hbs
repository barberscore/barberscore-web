{{#power-select
      options=contests
      selected=primary
      placeholder="Select Primary Contest"
      onchange=(action "savePrimary")
      allowClear=true
      as |primary|
}}
  {{primary.nomen}}
{{/power-select}}
