<section>
  {{bread-crumbs tagName="ol" outputStyle="bootstrap" linkable=true}}
</section>
<section>
  <div class='table-responsive'>
    <table class='table'>
      <caption>Current Sessions</caption>
      <thead>
        <tr>
          <th>Kind</th>
          <th>Convention</th>
        </tr>
      </thead>
      <tbody>
        {{#each sortedSessions as |session|}}
          <tr>
            <td>{{#link-to 'admin.contest-manager.session.details' session}}{{session.kind}}{{/link-to}}</td>
            <td>{{session.convention.name}}</td>
          </tr>
        {{else}}
          <tr>
            <td>No current sessions</td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</section>
