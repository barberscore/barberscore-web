<section>
  <ul class='list-unstyled'>
    {{!-- <li>Prelims: {{boolean-helper model.is_prelims}}</li> --}}
    <li>Number Rounds: {{model.num_rounds}}</li>
    <li>Primary Award: {{model.primary.award.name}}</li>
  </ul>
</section>

{{!-- <section>
  {{#bs-form
    model=model
    formLayout='vertical'
    onSubmit=(action 'saveSession')
    as |form|
  }}
    {{#form.element property='is_prelims' as |el|}}
      {{#bs-button-group
        value=el.value
        type='radio'
        onChange=(action (mut el.value))
        as |bg|
      }}
        {{#bg.button type='default' value=false disabled=isDisabled}}No Prelims{{/bg.button}}
        {{#bg.button type='default' value=true disabled=isDisabled}}Yes Prelims{{/bg.button}}
      {{/bs-button-group}}
    {{/form.element}}
    {{#form.element property='num_rounds' useIcons=false as |el|}}
      {{#power-select
        options=model.numOptions
        selected=el.value
        onchange=(action (mut el.value))
        placeholder="Number Rounds"
        disabled=isDisabled
        allowClear=true
        as |num_rounds|
      }}
        {{num_rounds}}
      {{/power-select}}
    {{/form.element}}
    {{#form.element property='primary' useIcons=false as |el|}}
      {{#power-select
            options=model.contests
            selected=el.value
            placeholder="Select Primary Contest"
            onchange=(action (mut el.value))
            disabled=isDisabled
            allowClear=true
            as |primary|
      }}
        {{primary.nomen}}
      {{/power-select}}
    {{/form.element}}
    {{#if isEditing}}
      {{#bs-button type='default' onClick=(action 'cancelSession')}}Cancel{{/bs-button}}
      {{#bs-button type='primary' buttonType="submit"}}Save{{/bs-button}}
      {{#bs-button type='danger' onClick=(action 'deleteSession') class='pull-right'}}Delete{{/bs-button}}
    {{else}}
      {{#bs-button onClick=(action 'editSession')}}Edit{{/bs-button}}
    {{/if}}
  {{/bs-form}}
</section>
 --}}