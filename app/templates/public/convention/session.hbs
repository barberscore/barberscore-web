{{#if model.current}}
  <div class='panel panel-default'>
    <div class='panel-heading'>
      <h4 {{action 'collapseHeader'}}>
        {{#if isCurrentCollapsed}}
          {{fa-icon 'caret-right'}}
        {{else}}
          {{fa-icon 'caret-down'}}
        {{/if}}
        &nbsp;{{model.kind}} {{model.current.kind}}
      </h4>
    </div>
    {{#bs-collapse collapsed=isCurrentCollapsed}}
      <div class='panel-body'>
        <div class='table-responsive'>
          <table class='table'>
            <thead>
            <tr>
              <th {{action 'sortPerformancesBy' 'num:asc'}}>Slot</th>
              <th {{action 'sortPerformancesBy' 'performer.prelim:desc'}}>Prelim</th>
              <th {{action 'sortPerformancesBy' 'performer.group.name:asc'}}>Performer</th>
              <th>Songs</th>
              <th class='text-right'>Scheduled/Actual</th>
            </tr>
            </thead>
            <tbody>
              {{#each sortedPerformances as |performance|}}
                <tr>
                  <td>
                    {{performance.num}}
                  </td>
                  <td>
                    {{single-fixed performance.performer.prelim}}
                  </td>
                  <td>
                    {{group-modal model=performance.performer.group}}
                    {{#if (eq performance.performer.group.kind 'Chorus')}}{{performance.performer.group.chapter.name}}{{/if}}
                  </td>
                  {{!-- <td>
                    {{#link-to 'public.organizations.organization' performance.performer.representing}}{{performance.performer.representing.name}}{{/link-to}}
                  </td> --}}
                  <td>
                    {{#each performance.sortedSongs as |song|}}
                      {{truncate-text song.submission.title limit=50}}<br>
                    {{/each}}
                  </td>
                  <td class='text-right'>
                    {{moment-format performance.slot.onstage 'LT Z z'}}<br>
                    {{moment-format performance.actual_start 'LT Z z'}}
                  </td>
                </tr>
              {{/each}}
            </tbody>
          </table>
        </div>
      </div>
    {{/bs-collapse}}
  </div>
{{/if}}
{{#if (eq model.status 'Published')}}
<section>
<h3>
  Official Scoring Summary
</h3>
</section>
<section>
{{x-toggle showLabels=true offLabel='Points' onLabel='Score' toggle='letsGo'}}
<div class='table-responsive'>
<table class='table'>
  <thead>
  <tr>
  <th {{action 'sortPerformersBy' 'performerscore.rank:asc'}}>Rank</th>
  <th {{action 'sortPerformersBy' 'group.name:asc'}}>Group</th>
  <th>Songs</th>
  <th {{action 'sortPerformersBy' 'performerscore.mus_points:desc'}}class='text-right'>MUS</th>
  <th {{action 'sortPerformersBy' 'performerscore.prs_points:desc'}}class='text-right'>PRS</th>
  <th {{action 'sortPerformersBy' 'performerscore.sng_points:desc'}}class='text-right'>SNG</th>
  <th {{action 'sortPerformersBy' 'performerscore.total_points:desc'}}class='text-right'>Total</th>
  {{!-- {{#each model.contests as |contest|}}
    <th class='text-right'>{{contest.award.name}}</th>
  {{/each}} --}}
  </tr>
  </thead>
  <tbody>
  {{#each sortedPerformers as |performer|}}
    <tr>
    <td>
      {{performer.performerscore.rank}}
    </td>
    <td>
      {{group-modal model=performer.group}}
      {{#if (eq performer.group.kind 'Chorus')}}{{performer.group.chapter.name}}<br>{{/if}}
      {{#if (eq performer.group.kind 'Chorus')}}Men on Stage: {{performer.men}}{{/if}}
    </td>
    <td>
      {{#each performer.performances as |performance|}}
        {{#each performance.songs as |song|}}
          {{truncate-text song.submission.title limit=50}}<br>
        {{/each}}
        {{#if (not-eq performer.performances.length 1)}}<em>{{performance.round.kind}}</em><br>{{/if}}
      {{/each}}
      <strong>Total</strong>
    </td>
    <td class='text-right'>
      {{#each performer.performances as |performance|}}
        {{#each performance.songs as |song|}}
          {{#if isRaw}}{{song.songscore.mus_points}}{{else}}{{single-fixed song.songscore.mus_score}}{{/if}}<br>
        {{/each}}
        {{#if (not-eq performer.performances.length 1)}}<em>{{#if isRaw}}{{performance.performancescore.mus_points}}{{else}}{{single-fixed performance.performancescore.mus_score}}{{/if}}</em><br>{{/if}}
      {{/each}}
      <strong>{{#if isRaw}}{{performer.performerscore.mus_points}}{{else}}{{single-fixed performer.performerscore.mus_score}}{{/if}}</strong><br>

    </td>
    <td class='text-right'>
      {{#each performer.performances as |performance|}}
        {{#each performance.songs as |song|}}
          {{#if isRaw}}{{song.songscore.prs_points}}{{else}}{{single-fixed song.songscore.prs_score}}{{/if}}<br>
        {{/each}}
        {{#if (not-eq performer.performances.length 1)}}<em>{{#if isRaw}}{{performance.performancescore.prs_points}}{{else}}{{single-fixed performance.performancescore.prs_score}}{{/if}}</em><br>{{/if}}
      {{/each}}
      <strong>{{#if isRaw}}{{performer.performerscore.prs_points}}{{else}}{{single-fixed performer.performerscore.prs_score}}{{/if}}</strong><br>
    </td>
    <td class='text-right'>
      {{#each performer.performances as |performance|}}
        {{#each performance.songs as |song|}}
          {{#if isRaw}}{{song.songscore.sng_points}}{{else}}{{single-fixed song.songscore.sng_score}}{{/if}}<br>
        {{/each}}
        {{#if (not-eq performer.performances.length 1)}}<em>{{#if isRaw}}{{performance.performancescore.sng_points}}{{else}}{{single-fixed performance.performancescore.sng_score}}{{/if}}</em><br>{{/if}}
      {{/each}}
      <strong>{{#if isRaw}}{{performer.performerscore.sng_points}}{{else}}{{single-fixed performer.performerscore.sng_score}}{{/if}}</strong><br>
    </td>
    <td class='text-right'>
      {{#each performer.performances as |performance|}}
        {{#each performance.songs as |song|}}
          {{#if isRaw}}{{song.songscore.total_points}}{{else}}{{single-fixed song.songscore.total_score}}{{/if}}<br>
        {{/each}}
        {{#if (not-eq performer.performances.length 1)}}<em>{{#if isRaw}}{{performance.performancescore.total_points}}{{else}}{{single-fixed performance.performancescore.total_score}}{{/if}}</em><br>{{/if}}
      {{/each}}
      <strong>{{#if isRaw}}{{performer.performerscore.total_points}}{{else}}{{single-fixed performer.performerscore.total_score}}{{/if}}</strong><br>
    </td>
    </tr>
  {{/each}}
  {{#each model.sortedSecondaryPerformers as |performer|}}
    <tr>
    <td>
      {{performer.performerscore.rank}}
    </td>
    <td>
      {{performer.group.name}}<br>
      {{#if (eq performer.group.kind 'Chorus')}}{{performer.group.chapter.name}}{{/if}}
    </td>
    <td>
      {{#each performer.performances as |performance|}}
        {{#each performance.songs as |song|}}
          {{truncate-text song.submission.title limit=50}}<br>
        {{/each}}
        {{#if (not-eq performer.performances.length 1)}}<em>{{performance.round.kind}}</em><br>{{/if}}
      {{/each}}
      <strong>Total</strong>
    </td>
    <td class='text-right'>
      {{#each performer.performances as |performance|}}
        {{#each performance.songs as |song|}}
          {{#if isRaw}}{{song.songscore.mus_points}}{{else}}{{single-fixed song.songscore.mus_score}}{{/if}}<br>
        {{/each}}
        {{#if (not-eq performer.performances.length 1)}}<em>{{#if isRaw}}{{performance.performancescore.mus_points}}{{else}}{{single-fixed performance.performancescore.mus_score}}{{/if}}</em><br>{{/if}}
      {{/each}}
      <strong>{{#if isRaw}}{{performer.performerscore.mus_points}}{{else}}{{single-fixed performer.performerscore.mus_score}}{{/if}}</strong><br>

    </td>
    <td class='text-right'>
      {{#each performer.performances as |performance|}}
        {{#each performance.songs as |song|}}
          {{#if isRaw}}{{song.songscore.prs_points}}{{else}}{{single-fixed song.songscore.prs_score}}{{/if}}<br>
        {{/each}}
        {{#if (not-eq performer.performances.length 1)}}<em>{{#if isRaw}}{{performance.performancescore.prs_points}}{{else}}{{single-fixed performance.performancescore.prs_score}}{{/if}}</em><br>{{/if}}
      {{/each}}
      <strong>{{#if isRaw}}{{performer.performerscore.prs_points}}{{else}}{{single-fixed performer.performerscore.prs_score}}{{/if}}</strong><br>
    </td>
    <td class='text-right'>
      {{#each performer.performances as |performance|}}
        {{#each performance.songs as |song|}}
          {{#if isRaw}}{{song.songscore.sng_points}}{{else}}{{single-fixed song.songscore.sng_score}}{{/if}}<br>
        {{/each}}
        {{#if (not-eq performer.performances.length 1)}}<em>{{#if isRaw}}{{performance.performancescore.sng_points}}{{else}}{{single-fixed performance.performancescore.sng_score}}{{/if}}</em><br>{{/if}}
      {{/each}}
      <strong>{{#if isRaw}}{{performer.performerscore.sng_points}}{{else}}{{single-fixed performer.performerscore.sng_score}}{{/if}}</strong><br>
    </td>
    <td class='text-right'>
      {{#each performer.performances as |performance|}}
        {{#each performance.songs as |song|}}
          {{#if isRaw}}{{song.songscore.total_points}}{{else}}{{single-fixed song.songscore.total_score}}{{/if}}<br>
        {{/each}}
        {{#if (not-eq performer.performances.length 1)}}<em>{{#if isRaw}}{{performance.performancescore.total_points}}{{else}}{{single-fixed performance.performancescore.total_score}}{{/if}}</em><br>{{/if}}
      {{/each}}
      <strong>{{#if isRaw}}{{performer.performerscore.total_points}}{{else}}{{single-fixed performer.performerscore.total_score}}{{/if}}</strong><br>
    </td>
    </tr>
  {{/each}}
  </tbody>
</table>
</div>
</section>
{{!-- <section>
  <h4>Contests</h4>
    <ul class='list-unstyled'>
      {{#each model.sortedChampionshipContests as |contest|}}
        <li>{{contest.award.name}} Champion: {{contest.contestscore.champion}}</li>
      {{/each}}
    </ul>
    <ul class='list-unstyled'>
      {{#each model.sortedQualificationContests as |contest|}}
        <li><strong>{{contest.award.name}} Qualifiers:</strong>
          {{#each contest.contestants as |contestant|}}
            {{contestant.performer.group.name}}{{#unless (eq contest.contestants.lastObject contestant)}},{{/unless}}
          {{/each}}
        </li>
      {{/each}}
    </ul>
</section> --}}
<section>
  <h4>Judging Panel</h4>
  <ul class='list-unstyled'>
    {{#each-in (group-by "category" model.sortedOfficialAssignments) as |category officialAssignments|}}
      <li><strong>{{category}}</strong>
        {{#each officialAssignments as |assignment|}}
          {{assignment.judge.person.common_name}}{{#unless (eq officialAssignments.lastObject assignment)}},{{/unless}}
        {{/each}}
      </li>
    {{/each-in}}
  </ul>
</section>
{{/if}}
