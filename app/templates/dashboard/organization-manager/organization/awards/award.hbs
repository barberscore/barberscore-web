<section>
  <div class='panel panel-default'>
    <div class='panel-heading'>
      <ul class='list-inline pull-right'>
        <li>{{#bs-button disabled=isPrevDisabled onClick=(action 'previousItem' model)}}Previous{{/bs-button}}</li>
        <li>{{#bs-button disabled=isNextDisabled onClick=(action 'nextItem' model)}}Next{{/bs-button}}</li>
      </ul>
      <h4>
        {{model.nomen}}
      </h4>
    </div>
    <div class='panel-body'>
      <section>
        {{#bs-form
          model=model
          formLayout='vertical'
          as |form|
        }}
          <ul class='list-inline'>
            <li>
              {{#form.element}}
                {{#bs-button disabled=isDisabled active=model.isPrimary onClick=(action (toggle 'isPrimary' model))}}
                  Primary
                {{/bs-button}}
              {{/form.element}}
            </li>
            <li>
              {{#form.element}}
                {{#bs-button disabled=isDisabled active=model.isNovice onClick=(action (toggle 'isNovice' model))}}
                  Novice
                {{/bs-button}}
              {{/form.element}}
            </li>
            <li>
              {{#form.element}}
                {{#bs-button disabled=isDisabled active=model.isImproved onClick=(action (toggle 'isImproved' model))}}
                  Most-Improved
                {{/bs-button}}
              {{/form.element}}
            </li>
            <li>
              {{#form.element}}
                {{#bs-button disabled=isDisabled active=model.isManual onClick=(action (toggle 'isManual' model))}}
                  Manual
                {{/bs-button}}
              {{/form.element}}
            </li>
          </ul>
          {{#form.element property='kind' useIcons=false as |el|}}
            {{#power-select
                  options=model.kindOptions
                  selected=el.value
                  placeholder="Select Award Kind"
                  onchange=(action (mut el.value))
                  disabled=isDisabled
                  allowClear=true
                  as |kind|
            }}
              {{kind}}
            {{/power-select}}
          {{/form.element}}
          {{!-- {{#form.element property='season' useIcons=false as |el|}}
            {{#power-select
                  options=model.seasonOptions
                  selected=el.value
                  placeholder="Select Award Season"
                  onchange=(action (mut el.value))
                  disabled=isDisabled
                  allowClear=true
                  as |season|
            }}
              {{season}}
            {{/power-select}}
          {{/form.element}} --}}
          {{form.element
            controlType='tel'
            placeholder='Number of Championship Rounds'
            property='championshipRounds'
            disabled=isDisabled
          }}
          {{form.element
            controlType='tel'
            placeholder='Number of Qualifier Rounds'
            property='qualifierRounds'
            disabled=isDisabled
          }}
          {{form.element
            controlType='tel'
            placeholder='Threshold'
            property='threshold'
            disabled=isDisabled
          }}
          {{form.element
            controlType='tel'
            placeholder='Minimum'
            property='minimum'
            disabled=isDisabled
          }}
          {{form.element
            controlType='tel'
            placeholder='Advance'
            property='advance'
            disabled=isDisabled
          }}
          {{form.element
            controlType='text'
            placeholder='Description'
            property='description'
            disabled=isDisabled
          }}
          {{#if isEditing}}
            {{#bs-button type='primary' buttonType="submit"}}Save{{/bs-button}}
            {{#bs-button type='default' onClick=(action 'cancelAward')}}Cancel{{/bs-button}}
            {{#bs-button type='danger' onClick=(action 'deleteAward') class='pull-right'}}Delete{{/bs-button}}
          {{else}}
            {{#bs-button onClick=(action 'editAward') disabled=isWrite}}Edit{{/bs-button}}
          {{/if}}
        {{/bs-form}}
      </section>
    </div>
  </div>
</section>