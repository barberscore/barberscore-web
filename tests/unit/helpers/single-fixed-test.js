import { singleFixed } from '../../../helpers/single-fixed';
import { module, test } from 'qunit';

module('Unit | Helper | single fixed');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = singleFixed([42]);
  assert.ok(result);
});
